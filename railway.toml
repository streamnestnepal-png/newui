[build]
builder = "DOCKERFILE"

[deploy]
preDeployCommand = "/usr/local/bin/init-mysql"
startCommand = "sh -c 'php -S 0.0.0.0:${PORT:-8080} -t /var/www/html /var/www/html/router.php'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
